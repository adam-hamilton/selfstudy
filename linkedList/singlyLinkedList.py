#creat nodes
#create linked list
#add nodes to linked list
#print linked list

class Node(object):
    def __init__(self, data)
        self.data = data
        self.next = None


class LinkedList(object):
    pass

#node => data, next
firstNode = Node("John")
linkedList = LinkedList()